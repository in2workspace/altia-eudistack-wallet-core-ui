name: PR quality

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  quality:
    uses: in2workspace/eudistack-devops-workflows/.github/workflows/spa-pr.yml@6dead093a3a5aba68940eda4958afd4f3a6a3702
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  license-compliance:
    uses: in2workspace/eudistack-devops-workflows/.github/workflows/common-license-gate.yml@6dead093a3a5aba68940eda4958afd4f3a6a3702
    with:
      setup_java: false
      install_command: npm ci
      sbom_command: npm run sbom
      sbom_path: build/sbom/sbom.json

  analyze:
    uses: in2workspace/eudistack-devops-workflows/.github/workflows/common-codeql-javascript.yml@6dead093a3a5aba68940eda4958afd4f3a6a3702
